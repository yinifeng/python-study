[project]
name = "python-study"
version = "1.0.0"
description = "这是一个学习python的项目"
readme = "README.md"
# 定义兼容的 Python 版本范围
requires-python = ">=3.9"
dependencies = [
    "flask>=3.1.3",
    "pandas>=2.3.3",
]
